/**
 * {
 *  Greeter: Greeter Function,
 *  general: general Function,
 * }
 */

// Greeter
const greeter = require("./greeter");

// Greeter instance
const g = new greeter.x("Heni");
g.sayHi();
// ---------------------------
greeter.y();
